# ✅ 網頁版 → Swift Typing 訊號修復完成

## 已完成的所有修改

### 1. Swift App 修改

#### MessageManager.swift
```swift
func sendTypingIndicator() {
    typingTimer?.invalidate()
    let conversationId = self.conversationId

    // 發送 typing 訊號到後端 API
    Task {
        do {
            try await APIClient.shared.sendTypingIndicator(conversationId: conversationId)
            AppLogger.shared.debug("⌨️ 發送輸入中訊號 - 對話ID: \(conversationId)")
        } catch {
            AppLogger.shared.error("❌ 發送輸入中訊號失敗", error: error)
        }
    }

    // 3 秒後自動停止
    typingTimer = Timer.scheduledTimer(withTimeInterval: 3.0, repeats: false) { _ in
        // Timer 只是用來限制發送頻率
    }
}
```

#### WebSocketManager.swift

**新增事件處理：**
```swift
case "user.typing", ".user.typing":
    handleUserTyping(json)
```

**新增 handleUserTyping() 方法：**
```swift
private func handleUserTyping(_ json: [String: Any]) {
    let data: Data

    if let dataString = json["data"] as? String {
        guard let d = dataString.data(using: .utf8) else { return }
        data = d
    } else if let dataDict = json["data"] as? [String: Any] {
        guard let d = try? JSONSerialization.data(withJSONObject: dataDict) else { return }
        data = d
    } else {
        return
    }

    do {
        let decoder = JSONDecoder()
        decoder.keyDecodingStrategy = .convertFromSnakeCase

        let typingEvent = try decoder.decode(TypingEvent.self, from: data)

        eventPublisher.send(.typing(typingEvent))

        AppLogger.shared.debug("⌨️ 收到輸入中訊號 - 使用者ID: \(typingEvent.userId), 對話ID: \(typingEvent.conversationId), 輸入中: \(typingEvent.isTyping)")

    } catch {
        AppLogger.shared.error("解析輸入中事件失敗", error: error)
    }
}
```

**新增頻道 log：**
```swift
if let channel = json["channel"] as? String {
    AppLogger.shared.debug("📺 事件頻道: \(channel)")
}
```

### 2. 後端修改

#### UserTyping.php
```php
public function __construct(
    public User $user,
    public int $conversationId
) {
    \Log::debug('📡 UserTyping 事件建立', [
        'user_id' => $user->id,
        'user_name' => $user->name,
        'conversation_id' => $conversationId,
        'channel' => 'private-conversation.'.$conversationId,
        'event_name' => 'user.typing'
    ]);
}
```

#### message-input.blade.php
```php
public function typing()
{
    \Log::debug('🔔 網頁版發送 typing 訊號', [
        'user_id' => auth()->id(),
        'conversation_id' => $this->conversationId
    ]);

    broadcast(new \App\Events\UserTyping(auth()->user(), $this->conversationId))->toOthers();
}
```

### 3. 網頁版修改

#### message-list.blade.php
```javascript
this.typingSound = new Audio('/typing.wav');  // 修正檔案名稱
```

### 4. 檔案清理

- ✅ 刪除重複的 `/Resources/typing.wav`
- ✅ 保留 `/Resource/typing.wav`
- ✅ 清除 Xcode DerivedData
- ✅ 清除 Laravel 快取

## 🧪 立即測試步驟

### 步驟 1: 準備環境

**終端機 1 - 啟動 Reverb：**
```bash
cd /Users/a123/PhpstormProjects/TeamChatUP-backend
php artisan reverb:start
```

**終端機 2 - 監控 Log：**
```bash
cd /Users/a123/PhpstormProjects/TeamChatUP-backend
tail -f storage/logs/laravel.log | grep -E "typing|UserTyping|🔔|📡"
```

### 步驟 2: 啟動 Swift App

1. 在 Xcode 中 Clean Build (Shift + Command + K)
2. 運行應用程式 (Command + R)
3. 登入使用者 B（例如：user_id = 2）
4. 進入對話 ID = 1
5. **保持 Xcode Console 開啟**

### 步驟 3: 啟動網頁版

1. 開啟瀏覽器：https://teamchatup-backend.test/chat
2. 登入使用者 A（例如：user_id = 1）
3. 進入相同的對話 ID = 1
4. 開啟 DevTools (F12) → Console 標籤

### 步驟 4: 執行測試

**在網頁版輸入框開始輸入文字**

### 步驟 5: 觀察結果

#### 終端機 2 應該顯示：
```
[🔔 DEBUG] 網頁版發送 typing 訊號 - user_id: 1, conversation_id: 1
[📡 DEBUG] UserTyping 事件建立 - user_id: 1, conversation_id: 1
```

#### 瀏覽器 Console 應該顯示：
```
⌨️ 播放輸入中音效
```

#### Xcode Console 應該顯示：
```
[📡 DEBUG] 收到事件: user.typing
[📺 DEBUG] 事件頻道: private-conversation.1
[📦 DEBUG] 事件資料內容: {conversation_id = 1; is_typing = 1; user_id = 1; user_name = "User A";}
[⌨️ DEBUG] 收到輸入中訊號 - 使用者ID: 1, 對話ID: 1, 輸入中: true
[⌨️ DEBUG] 播放輸入中音效
```

#### Swift App 應該：
- 🔊 播放 typing 音效（短促的聲音）

## 🔍 故障排除

### 問題 1: Xcode Console 沒有任何 log

**檢查：**
```
搜尋: "WebSocket 連線成功"
搜尋: "訂閱成功: private-conversation"
```

**如果沒有：**
- WebSocket 未連線或未訂閱
- 重啟應用程式

### 問題 2: 終端機沒有 UserTyping log

**檢查：**
- Livewire 的 typing() 方法是否被調用
- 在瀏覽器 Console 手動測試：
  ```javascript
  Livewire.all()[0].call('typing')
  ```

### 問題 3: Swift 收到其他事件但沒有 user.typing

**檢查：**
- 搜尋 Xcode Console: "收到事件"
- 確認是否有 "message.sent" 但沒有 "user.typing"
- 檢查後端是否廣播（終端機 2）

### 問題 4: Swift 收到 user.typing 但顯示 "未處理的事件"

**檢查：**
- WebSocketManager.swift 第 325 行
- 確認包含：`case "user.typing", ".user.typing":`

### 問題 5: 處理事件但沒播放音效

**檢查：**
- 搜尋 Xcode Console: "收到輸入中訊號"
- 確認有此 log 但沒有 "播放輸入中音效"
- 檢查 MessageManager 的 handleWebSocketEvent()

### 問題 6: 播放音效但沒聲音

**檢查：**
```bash
ls -la /Users/a123/Documents/Xcode/TeamChatUP/TeamChatUP/Resource/typing.wav
```
- 確認檔案存在
- 檢查系統音量
- 測試其他音效是否正常

## 📊 完整的資料流程

```
網頁版輸入框
    ↓ wire:keydown.debounce.500ms="typing"
Livewire typing() 方法
    ↓ broadcast(new UserTyping(...))
Laravel Reverb 伺服器
    ↓ WebSocket 廣播到 private-conversation.1
Swift WebSocketManager
    ↓ 收到 user.typing 事件
handleUserTyping() 解析
    ↓ eventPublisher.send(.typing(...))
MessageManager.handleWebSocketEvent()
    ↓ 檢查是否為新使用者
SoundManager.playTypingSound()
    ↓ AVAudioPlayer 播放
🔊 音效輸出
```

## ✅ 驗證清單

測試完成後，確認以下項目：

- [ ] 終端機 2 顯示 "UserTyping 事件建立"
- [ ] 瀏覽器 Console 顯示 "⌨️ 播放輸入中音效"
- [ ] Xcode Console 顯示 "收到事件: user.typing"
- [ ] Xcode Console 顯示 "收到輸入中訊號"
- [ ] Xcode Console 顯示 "播放輸入中音效"
- [ ] Swift App 播放音效（聽到聲音）
- [ ] 持續輸入不會重複播放（防重複機制）
- [ ] 停止輸入後再次輸入會再次播放

## 🎯 預期結果

**成功標準：**
1. 網頁版輸入時，Swift App 聽到 typing 音效
2. 所有 log 都正確顯示
3. 音效只在新使用者開始輸入時播放一次

**如果成功：**
- ✅ 功能完全正常
- ✅ 可以開始使用

**如果失敗：**
- 回報在哪個步驟失敗
- 提供 Xcode Console 的完整 log
- 提供終端機 2 的 log

---

**狀態：** ✅ 所有程式碼修改完成
**下一步：** 按照上述步驟測試
**預計測試時間：** 5 分鐘
